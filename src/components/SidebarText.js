import { memo } from "react";

const SidebarText = memo(() => {
  return (
    <div className="text-81xl font-semibold font-h6-16px-regular-inter text-styles-login-sign-in text-left inline-block w-[374px] h-[121px]">
      Sidebar
    </div>
  );
});

export default SidebarText;
