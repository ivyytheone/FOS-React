import { memo } from "react";

const AuthenticationText = memo(() => {
  return (
    <div className="text-81xl font-semibold font-h6-16px-regular-inter text-styles-login-sign-in text-left">
      Authentication
    </div>
  );
});

export default AuthenticationText;
